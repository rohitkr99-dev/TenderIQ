services:
  - type: web
    name: tenderiq-ai
    runtime: node
    rootDir: tender-iq-ai
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: OPENAI_API_KEY
        sync: false
